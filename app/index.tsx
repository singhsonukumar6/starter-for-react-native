/**
 * Main Entry Point / Splash Screen
 * Redirects to login or home based on auth state
 */

import React from 'react';
import SplashScreen from '../src/screens/SplashScreen';

export default SplashScreen;
